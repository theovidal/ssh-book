package commands

import "github.com/urfave/cli"

// RemoveConnection is a CLI command that removes a connection from user's book.
// It is called when the "ssh-book remove" command is triggered by the user.
func RemoveConnection(c *cli.Context) error {
	return nil
}
