package commands

import "github.com/urfave/cli"

// Connect is a CLI command that opens a connection stored inside user's book.
// It is called when the "ssh-book connect" command is triggered by the user.
func Connect(c *cli.Context) error {
	return nil
}
